rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Só quem estiver logado (você e a Del) pode ler e escrever qualquer dado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
